<?xml version="1.0" encoding="utf-8"?>
<!--
    Politique réseau de Fimus : HTTPS obligatoire.

    Le HTTP en clair n'est toléré que pour les hôtes du serveur Laravel local
    utilisés en développement (cf. lib/config/app_environment.dart, enum
    DevHost : fimus.local, 10.0.2.2 pour l'émulateur Android, localhost).
    L'API de production (https://fimus.honowa.com) et tout autre hôte passent
    obligatoirement par TLS, y compris les images et pièces jointes.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">fimus.local</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
